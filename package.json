{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
     "buildClient" : "cd client && npm install",
    "buildServer" : "cd server && npm install",
    "browserifyClient" : " cd client && browserify app.js -o dist/bundle.js ",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http": "0.0.0"
  }
}
